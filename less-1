<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
        <script>
            'use stricrt'
            let Tc = +prompt('Введите температуру по Цельсию');
            let Tf = (9/5)*Tc+32;
            alert(Tf);
        </script>
        
        
        <script>
            'use stricrt'
            let name = 'Василий';
            let admin = name;
                console.log(admin);
        </script>

        <script>
        'use stricrt';
        /*
        1. две операции сложения и они равноправны, а значит пойдут друг за другом, слева направо.
        2. 10 + 10 даст 20 в первом действии
        3. далее код приобрете вид 20 + '10' а так как один из операндов строка, то все выражние будет приведено к строке.
        4. '20' + '10' таким образом обе строки склеятся в одну, приобретя значение '2010'
        */
        let result = 10 + 10 + '10';
        console.log(result);
        </script>

        <script>
            'use stricrt'
        /*
        1. похожая ситуация, две операции сложения, действия идут слева направо.
        2. сначала 10 + '10' таким образом оба операнда будут приведены к единому значению - строка. Получим '1010'
        3. далее  '1010' + 10 а так как один из операндов строка, то все выражние будет приведено к строке.
        4. в итоге получим '101010'
        */
        let result = 10 + '10' + 10;
        console.log(result);
        </script>

        <script>
        'use stricrt'    
        /*
        1. последний операнд имеет перед собой знак "+" - унарный плюс, он имеет высокий приоритет и переводит данное значение в число.
        2. теперь выраженивы выглядит так: 10 + 10 + 10
        3. все операнды являются числами и они просто суммируются
        4. получим в итоге число 30
        */
        let result = 10 + 10 + +'10';
        console.log(result);
        </script>

        <script>
            'use stricrt'
        /*
        1. второй операнд представляет собой скобки без значений, туда вкладывается ноль.
        2. знак "-" перед вторым операндом, это унарный ноль,к оторый рисваивается значению из скобок, получаем -0
        3. теперь наше выражение выглядит так: 10 / -0
        4. десять разделить на минус ноль
        5. в ответе получим -Infinity некое ультрамаленькое отрицательное число, но какое неизвестно, так как операция деления на ноль не определена.
        */
        let result = 10 / -'';
        console.log(result);
        </script>

        <script>
            'use stricrt'
        /*
        1. второй операнд имеет перед собой знак унарного плюса, по приоритету он выше чем операця деления.
        2. по идее, второй операнд должен был принять числовое значение и выглядеть вот так 2, 5 
        3. тогда выражение ипринимает вид 10 / 2, 5
        4. так как во втором опранде после запятой стоит пробел, то данное значение не является числом и при выполнении операции деления мы получим ответ NaN - десять делим на неизвестно что. 
        */
        let result = 10 / +'2, 5';
        console.log(result);
        </script>
</body>
</html>
